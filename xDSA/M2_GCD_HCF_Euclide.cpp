/*gcd(a, b) = gcd(a-b, b)
            = gcd(a%b, b)
We can did this with the help of Recursion- for better complexity

RelationShip Between GCD/HCF & LCM         
    LCM(a, b)*GCD(a, b) = a*b
        
*/

